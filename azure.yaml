name: rust-mcp-azure
metadata:
  template: rust-mcp-azure@0.1.0-beta
infra:
  provider: bicep
  path: infra
services:
  api:
    project: .
    language: docker
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
pipeline:
  provider: github